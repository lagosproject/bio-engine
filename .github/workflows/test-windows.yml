name: Windows Binary Verification

on:
  workflow_dispatch:
  push:
    branches: [ main, develop ]
    paths:
      - 'services/reference.py'
      - 'core/config.py'
      - '.github/workflows/test-windows.yml'
      - 'tests/test_windows_binaries.py'

jobs:
  test-windows:
    name: Verify on Windows
    runs-on: windows-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          while IFS= read -r line || [ -n "$line" ]; do
            line=$(echo "$line" | sed 's/#.*//' | xargs)
            [ -z "$line" ] && continue
            [[ "$line" =~ ^[[:space:]] ]] && continue
            pip install "$line" || echo "WARNING: Failed to install $line (may be platform-specific)"
          done < requirements.txt
          pip install h11 

      - name: Install Windows Sidecars (samtools & htslib)
        shell: pwsh
        run: |
          # Install Scoop
          Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
          Invoke-RestMethod -Uri https://get.scoop.sh | Invoke-Expression
          
          # Install bioinformatics tools
          scoop install samtools htslib
          
          # Determine binary locations
          $samtoolsPath = (Get-Command samtools.exe).Source
          $bgzipPath = (Get-Command bgzip.exe).Source
          
          Write-Host "Found samtools at: $samtoolsPath"
          Write-Host "Found bgzip at: $bgzipPath"
          
          # Add current scoop shim dir to PATH if not already there
          # (Scoop usually adds it to the user PATH, but for this session we might need to be explicit)
          $scoopShimDir = Join-Path $env:USERPROFILE "scoop\shims"
          echo "$scoopShimDir" >> $env:GITHUB_PATH

      - name: Run Windows Binary Test
        env:
          BIO_BGZIP_PATH: bgzip.exe
          BIO_SAMTOOLS_PATH: samtools.exe
          TRACY_PATH: echo "Mocking tracy" # We can mock tracy for this specific test if not needed
        run: |
          python tests/test_windows_binaries.py
